@page "/intervaltimers"

<h1>Interval Timers</h1>

@if (IntervalSets.Any())
{
    @foreach (IntervalSetItem intervalSet in IntervalSets)
    {
        <div>@intervalSet.SetName</div>
    }
}
else
{
    <div>There are no Interval sets</div>
}


@code {
    public List<IntervalSetItem> IntervalSets { get; set; } = new List<IntervalSetItem>();
}
